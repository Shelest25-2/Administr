#!/bin/bash — указывает, что это bash-скрипт

-d "$path" — проверяет, что путь существует и является директорией.
"$path"/*/ — ищет все подкаталоги в указанном пути. (путь + /подкаталог1/, путь + /подкаталог2/...)
basename "$dir" — извлекает имя подкаталога из полного пути.
ls -1q — выводит элементы в одну колонку (-1), игнорируя специальные символы (-q).

wc -l — считает количество строк (т.е. элементов).
-l - строк (пустые тоже считаются)
-c размер в байтах
-m кол-во символов
-w слов

| - пайп - передает выходной одной еоманды на вход другой
ls -1q "$dir" | wc -l  -  выводим файлы в подкаталоге, после - считаем количество строк в выводе
